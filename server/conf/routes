# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

GET     /h                          controllers.HelloController.health
GET     /cards                      controllers.CardController.getByEmployeeId(employee_id)
GET     /cards                      controllers.CardController.getAll
POST    /card                       controllers.CardController.sendCard
GET     /employees                  controllers.EmployeeController.getAll
GET     /employee                   controllers.EmployeeController.getById(id)